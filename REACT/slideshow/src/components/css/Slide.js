import styled from "styled-components";

// Estilamos los elementos párrafo "p"
export const Slide = styled.div`
  min-width: 100%;
  overflow: hidden;
  tansition: .3s ease all;
  z-index: 10;
  max-height: 500px;
  position: relative;

  img {
    width: 100%;
    vertical-align: top;
  }
`;
